@model Faculty
@{ 

    if (Model != null)
    {
        var image = @Model.imagePath;
        <div id="myDiv10" class="img" style="background-image:url(@Model.imagePath)">

        </div>
    }
}
